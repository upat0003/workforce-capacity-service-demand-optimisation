name: 'workforce_capacity_service_demand_optimisation'
version: '1.0.0'
config-version: 2

profile: 'wfcso'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  workforce_capacity_service_demand_optimisation:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts

vars:
  small_cohort_suppression_threshold: 8
